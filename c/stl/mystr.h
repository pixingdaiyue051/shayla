#ifndef _HEAD_MYSTR
#define _HEAD_MYSTR

/**
 * 计算字符串长度
 **/
int ht_str_len(char *s);

/**
 * 比较两个字符串是否相等
 */
int ht_str_cmp(char *s1, char *s2);

/**
 * 将字符串转换成数字 实现atoi
 */
int ht_str_2int(char *s);

#endif